plugins {
    id "org.springframework.boot" version "2.0.1.RELEASE"
}

dependencies {
    compile project(':domain-jpa')
    compile group: 'org.springframework.boot', name: 'spring-boot-starter', version: '2.0.1.RELEASE'
}
